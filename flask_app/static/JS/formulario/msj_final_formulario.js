let portada = document.getElementById("final-btn");
final.addEventListener("click", () => {
                let final_portada = document.forms["f_portada"];
                final_portada.submit();
        });

let formulario = document.getElementById("formulario");
formulario.addEventListener("click", () => {
                let final_formulario = document.forms["f_formulario"];
                final_formulario.submit();
        });